# 🛠️ วิธีแก้ไขปัญหาฐานข้อมูล Course Registration System

## 🚨 ปัญหาที่พบ
```
Fatal error: Unknown column 'username' in 'field list'
```

ปัญหานี้เกิดจากฐานข้อมูลไม่ได้ถูกสร้างหรือ import ถูกต้อง

## ✅ วิธีแก้ไข (เลือก 1 วิธี)

### 🎯 วิธีที่ 1: ใช้สคริปต์อัตโนมัติ (แนะนำ)

1. **เปิด web browser** ไปที่ `http://localhost/course-registration-system/database-repair.php`
2. **สคริปต์จะทำการ:**
   - ตรวจสอบและสร้างฐานข้อมูล
   - ตรวจสอบและสร้างตาราง users ที่ถูกต้อง
   - เพิ่มข้อมูลผู้ใช้เริ่มต้น
3. **รอจนเห็นข้อความ "การตรวจสอบเสร็จสมบูรณ์!"**
4. **ลองเข้าสู่ระบบใหม่**

### 🎯 วิธีที่ 2: Import ฐานข้อมูลใหม่

1. **เปิด phpMyAdmin** (`http://localhost/phpmyadmin`)
2. **ลบฐานข้อมูลเก่า** (ถ้ามี):
   - คลิกที่ฐานข้อมูล `course_registration`
   - คลิก "Drop" เพื่อลบ
3. **สร้างฐานข้อมูลใหม่**:
   - คลิก "New" 
   - ชื่อฐานข้อมูล: `course_registration`
   - Collation: `utf8mb4_unicode_ci`
   - คลิก "Create"
4. **Import ไฟล์ database.sql**:
   - เลือกฐานข้อมูล `course_registration`
   - คลิกแท็บ "Import"
   - เลือกไฟล์ `database.sql`
   - คลิก "Go"

### 🎯 วิธีที่ 3: ใช้ Command Line

```sql
-- เปิด MySQL command line
mysql -u root -p

-- ลบและสร้างฐานข้อมูลใหม่
DROP DATABASE IF EXISTS course_registration;
CREATE DATABASE course_registration;
USE course_registration;

-- Import ไฟล์
SOURCE path/to/database.sql;
```

## 🔑 ข้อมูลสำหรับทดสอบ Login

หลังจากแก้ไขเรียบร้อย ใช้ข้อมูลนี้เข้าสู่ระบบ:

### 👨‍💼 Admin Account
- **Username**: `admin`
- **Password**: `admin123`

### 👨‍🎓 Student Accounts
- **Username**: `student1`, `student2`, `student3`
- **Password**: `123456` (สำหรับทุก student)

## 🔍 การตรวจสอบว่าแก้ไขแล้ว

1. **เข้าสู่ระบบ**: `http://localhost/course-registration-system/login.php`
2. **ลองใส่ username**: `admin` password: `admin123`
3. **ควรเข้าสู่ระบบได้โดยไม่มี error**

## ⚠️ หมายเหตุสำคัญ

- **ตรวจสอบ config.php**: ให้แน่ใจว่าการตั้งค่าฐานข้อมูลถูกต้อง
- **XAMPP**: ให้แน่ใจว่า Apache และ MySQL เปิดอยู่
- **Backup**: สำรองข้อมูลก่อนลบฐานข้อมูล (ถ้ามีข้อมูลสำคัญ)

## 🆘 ยังไม่ได้?

ถ้ายังมีปัญหา ตรวจสอบ:

1. **การตั้งค่าใน config.php**:
```php
$db_host = 'localhost';    // ✅ ถูกต้อง
$db_user = 'root';         // ✅ ถูกต้อง  
$db_pass = '';             // ✅ ถูกต้อง (XAMPP default)
$db_name = 'course_registration'; // ✅ ต้องตรงกัน
```

2. **ตรวจสอบ MySQL service** ใน XAMPP Control Panel
3. **ดู error log** ใน XAMPP logs folder

---
**หลังจากแก้ไขเรียบร้อย ระบบควรทำงานได้ปกติ! 🎉**
